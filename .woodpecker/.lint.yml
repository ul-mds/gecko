steps:
  lint:
    image: python:3.12
    commands:
      - python -m pip install poetry
      - poetry install
      - poetry run black --check --no-color
    when:
      path:
        include: [ ".woodpecker/*.yml", "geco/**/*.py", "tests/**/*.py", "pyproject.toml" ]