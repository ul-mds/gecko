steps:
  lint:
    image: python:3.12
    commands:
      - python -m pip install poetry
      - poetry install
      - poetry run ruff check .
      - poetry run ruff format --check .
    when:
      path:
        include: [ ".woodpecker/*.yml", "gecko/**/*.py", "tests/**/*.py", "pyproject.toml" ]